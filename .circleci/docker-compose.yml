version: "3.9"

services:
  web:
    image: node:20
    container_name: nextjs_app
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "3000:3000"
    env_file:
      - .env
    command: sh -c "npm install && npm run dev"
